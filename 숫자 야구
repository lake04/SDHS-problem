using System;

namespace MyCompiler
{
    class Program
    {
        public static void Main(string[] args)
        {
            Random random = new Random();

            string answer = "";
            int count = 0;

            answer += random.Next(1, 10).ToString();

            while (answer.Length < 3)
            {
                string number = random.Next(0, 10).ToString();

                if (!answer.Contains(number))
                {
                    answer += number;
                }
            }

            while (true)
            {
                int strike = 0;
                int ball = 0;

                Console.Write("사용자 입력: ");
                string guess = Console.ReadLine();

                if (guess.Length != 3)
                {
                    Console.WriteLine("세 자리 숫자를 입력하세요.");
                    continue;
                }

                count++;

                for (int i = 0; i < 3; i++)
                {
                    if (guess[i] == answer[i])
                    {
                        strike++;
                    }
                    else
                    {
                        for (int j = 0; j < 3; j++)
                        {
                            if (guess[i] == answer[j])
                            {
                                ball++;
                                break;
                            }
                        }
                    }
                }

                if (strike == 3)
                {
                    Console.WriteLine("결과: 정답입니다! (총 시도 횟수: "+ count+ "회)");
                    break;
                }

                Console.WriteLine("결과: "+ strike+ " Strike, "+ ball+ " Ball");
            }
        }
    }
}
